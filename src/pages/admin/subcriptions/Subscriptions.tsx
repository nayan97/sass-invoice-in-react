import React from "react";

const Subscriptions: React.FC = () => {


    return (
        <div>
            Active States: If any of the nested links match the active URL path, the main dropdown menu text subtly changes styling to signal focus to the user.
        </div>
    );
};

export default Subscriptions;
